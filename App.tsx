import { VideoView } from '@/screens';
import { View } from 'react-native';

function App(): React.JSX.Element {
  return (
    <View style={{ flex: 1 }}>
      <VideoView />
    </View>
  );
}

export default App;
